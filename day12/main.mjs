// main.js

import {add, subtract,multiply,divide} from `/cals.js`;

const a=19;
const b=21;

console.log(`add: ${add(a,b)}`);

console.log(`subtract: ${subtract(a,b)}`);

console.log(`multiply: ${multiply(a,b)}`);

console.log(`divide: ${divide(a,b)}`);